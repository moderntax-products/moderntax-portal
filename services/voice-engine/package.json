{
  "name": "moderntax-voice-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Custom IRS PPS call engine: deterministic IVR + hold sentinel + Claude agent, with checkpointed resume.",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx watch src/server.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.113.0",
    "@supabase/supabase-js": "^2.45.0",
    "twilio": "^5.3.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
